import 'package:flutter/material.dart';

class HouseworkScreen extends StatelessWidget {
   
  const HouseworkScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('HouseworkScreen'),
      ),
    );
  }
}